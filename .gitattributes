# Normalize line endings to LF in the repo. WoW reads LF fine, and the CI shell
# script (Tests/ci/check-dev-token.sh) runs on ubuntu — CRLF there
# would break it.
* text=auto eol=lf
*.sh text eol=lf
