# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Packager output
.release/

# Generated dev-build TOC (STU-074) — never commit, never ship.
Foundry-1.0_DevBuild.toc

# Git worktrees (studio standard: feature branches live in .worktrees/)
.worktrees/

# Wiki staging. The GitHub wiki is a SEPARATE repo (Foundry.wiki.git).
# Drafts live here for review, then publish to the wiki repo — they are not
# part of the code repo's history.
.wiki/

# Private dev environment (separate repo: Royaleint/Foundry_Dev) — tracker,
# plans, and dev notes. Never part of the public code repo's history.
Foundry_Dev/
